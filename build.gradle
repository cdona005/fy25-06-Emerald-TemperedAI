plugins {
  id 'java'
  id 'application'
}

repositories { mavenCentral() }

dependencies {
  implementation 'com.fasterxml.jackson.core:jackson-databind:2.17.1'
}

application {
  // Use the fully qualified name if it’s in a package
  mainClass = 'OllamaGenerate'
}
